import java.lang.reflect.Array;

public class IntersectionGridReservation {
    //For reserving the paths in an intersection we divide our intersection into a grid of x by y boxes
    //Each box will contain a reservation start time and end time.
    Array GridReservations;
    public IntersectionGridReservation(intersection ourIntersection, int x, int y){


    }
    public boolean ReservePath(intersectionPath path, float speed,float startTime){
        //Keep an float for newest time possible for the given path.
        float bestTime=1000000;
        //Get discrete coordinates for path

        //Iterate through the discrete coordinates and check for the reserved time for each tile
        return false;
    }
}
